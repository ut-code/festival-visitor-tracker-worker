{
  "name": "visitor-tracker",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "lefthook": "^1.8.4"
  },
  "scripts": {
    "prepare": "cd worker; bun i --frozen-lockfile && cd ../view && bun i --frozen-lockfile"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
